node_modules/
.DS_Store
*.log
.playwright-mcp/

# Build artifact: regenerated by `node scrape.mjs` on every run and published
# straight to Pages. Committing 4 MB twice a week would bloat the repo, while
# the weekly snapshots under data/history/ keep the durable price record.
data/offers.json
